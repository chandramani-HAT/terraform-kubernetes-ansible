---
- name: Run common tasks on all nodes
  hosts: all
  gather_facts: yes
  roles:
    - common

- name: Configure master node
  hosts: master
  roles:
    - k8s_master

- name: Configure worker nodes
  hosts: worker_group
  roles:
    - k8s_worker
